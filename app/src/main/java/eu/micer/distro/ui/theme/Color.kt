package eu.micer.distro.ui.theme

import androidx.compose.ui.graphics.Color

// Main Colors
val Primary = Color(0xFF1F2937)
val OnPrimary = Color(0xFFFFFFFF)

// Primary Container (lighter variant for cards, buttons, highlights)
val PrimaryContainer = Color(0xFFF3F4F6)
val OnPrimaryContainer = Color(0xFF1F2937)

// Surface Colors (backgrounds)
val Surface = Color(0xFFFAFAFA)
val OnSurface = Color(0xFF111827)
val SurfaceVariant = Color(0xFFE5E7EB)
val OnSurfaceVariant = Color(0xFF6B7280)

// Background
val Background = Color(0xFFFAFAFA)
val OnBackground = Color(0xFF111827)

// Accent (used for secondary highlights)
val Secondary = Color(0xFF6B7280)
val OnSecondary = Color(0xFFFFFFFF)
val SecondaryContainer = Color(0xFFE5E7EB)
val OnSecondaryContainer = Color(0xFF1F2937)

// Success (for installed status, completed downloads)
val Success = Color(0xFF10B981)
val OnSuccess = Color(0xFFFFFFFF)

// Error (for delete actions, errors, uninstall)
val Error = Color(0xFFEF4444)
val OnError = Color(0xFFFFFFFF)
val ErrorContainer = Color(0xFFFEE2E2)
val OnErrorContainer = Color(0xFF991B1B)

// Outline (for borders)
val Outline = Color(0xFFD1D5DB)
